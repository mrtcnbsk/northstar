---
"@ilura/northstar": minor
---

Local/openai-compatible provider authoring (BYOK + presets — Ollama, LM Studio, or a custom openai-compatible endpoint) via the "Add a local provider" TUI dialog and global auth store, with local-model capability validation: an unverified model (unknown context window or tool-call support) surfaces a visible warning and keeps automatic compaction off.
